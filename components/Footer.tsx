import React from 'react';

export const Footer: React.FC = () => {
  return (
    <footer className="bg-white py-6 border-t border-slate-100 hidden">
      {/* Footer content is now integrated into Contact section for cleaner flow */}
    </footer>
  );
};